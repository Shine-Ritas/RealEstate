<section class="border-t border-outline bg-surface py-10 md:py-12">
    <div class="container mx-auto px-4 md:px-6 lg:px-8">
        <div class="flex flex-col gap-4 md:flex-row md:items-center md:justify-between">
            <h2 class="text-xl font-semibold text-on-surface md:text-2xl">
                {{ __('guest.discover_heading') }}
            </h2>
            <div class="flex flex-col gap-1 text-right text-on-surface-variant sm:flex-row sm:gap-6">
                <span>{{ __('guest.contact_address') }}</span>
                <span>{{ __('guest.contact_phone') }}</span>
            </div>
        </div>
    </div>
</section>
